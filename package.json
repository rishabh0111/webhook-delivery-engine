{
  "name": "webhook-delivery-engine",
  "version": "0.1.0",
  "description": "A self-hostable webhook delivery engine: durable persistence, signed deliveries, retries, and dead-lettering.",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "migrate": "node --env-file-if-exists=.env src/migrate.js",
    "test": "jest --runInBand"
  },
  "engines": {
    "node": ">=20.12"
  },
  "license": "MIT",
  "dependencies": {
    "bullmq": "^5.79.3",
    "express": "^4.19.2",
    "ioredis": "^5.11.1",
    "pg": "^8.12.0",
    "pino": "^9.4.0",
    "pino-http": "^10.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
